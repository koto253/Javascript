// for loops
function forLoop(lowNum, highNum) {
    for(let i =lowNum; i <=highNum; i++) {
        console.log(i)
    }
}
forLoop(-1, 2);

console.log(`##########################`);

function whileLoop(lowNum, highNum) {
   while(lowNum <= highNum) {
       console.log(lowNum);
       lowNum++;
   }
}
whileLoop(-1, 2)

